{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "installCommand": "npm install",
    "buildCommand": "CI=false npm run build"
  },
  "deploy": { "startCommand": "npx serve --single --listen $PORT build" }
}
