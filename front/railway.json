{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "installCommand": "npm install",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "serve --single --listen $PORT build"
  }
}